const inicialState = {
    drivers: ''
}

function reducer(state = inicialState, action) {
  switch (action.type) {
    case value:
        
  
    default:
        return state;
  }
}

export default reducer